#include <iostream>

class Helper {
	public:
		int lineSearch(int arrayData[], int arrayLength, int valueToSearch);
		int binarySerch(int arrayData[], int startPosition, int endPosition, int valueToSearch);
};